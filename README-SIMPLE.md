# Flask CMS - نصب ساده روی Red Hat 8.10

## 🎯 هدف
نصب و راه‌اندازی Flask CMS روی سرور Red Hat 8.10 با کمترین تلاش

## 🚀 نصب سریع

### مرحله 1: آماده‌سازی پروژه (اختیاری)
```bash
# اگر می‌خواهید وابستگی‌ها را بسته‌بندی کنید
./bundle-dependencies.sh
```

### مرحله 2: انتقال به سرور
```bash
# کپی پروژه به سرور
scp -r flask-cms-itop/ user@server:/tmp/
```

### مرحله 3: نصب روی سرور
```bash
# روی سرور
cd /tmp/flask-cms-itop
sudo ./install-simple.sh
```

## 📦 محتویات پروژه

### فایل‌های اصلی
- `app.py` - برنامه اصلی Flask
- `requirements.txt` - وابستگی‌های Python
- `config.py` - تنظیمات برنامه
- `models.py` - مدل‌های دیتابیس
- `forms.py` - فرم‌های برنامه

### اسکریپت‌های نصب
- `install-simple.sh` - اسکریپت نصب ساده
- `bundle-dependencies.sh` - بسته‌بندی وابستگی‌ها
- `install.sh` - اسکریپت نصب کامل

### فایل‌های پیکربندی
- `nginx/` - تنظیمات Nginx
- `production.env` - متغیرهای محیطی
- `Dockerfile` - (اختیاری) برای Docker

## 🔧 پیش‌نیازها

### مشخصات سرور
- **سیستم عامل:** Red Hat Enterprise Linux 8.10
- **معماری:** x86_64
- **حداقل RAM:** 4GB
- **حداقل فضای دیسک:** 20GB

### دسترسی مورد نیاز
- دسترسی root یا sudo
- اتصال اینترنت (فقط برای نصب پکیج‌های سیستم)

## 🌐 دسترسی به برنامه

### URL های مهم
- **وب سایت:** http://localhost
- **Health Check:** http://localhost/health

### اطلاعات ورود پیش‌فرض
- **نام کاربری:** admin
- **رمز عبور:** admin123

## 🛠️ دستورات مدیریت

### مدیریت سرویس‌ها
```bash
# وضعیت سرویس‌ها
systemctl status flask-cms postgresql-15 redis6 nginx

# راه‌اندازی مجدد
systemctl restart flask-cms

# متوقف کردن
systemctl stop flask-cms

# مشاهده لاگ‌ها
journalctl -u flask-cms -f
```

### مدیریت دیتابیس
```bash
# بکاپ
sudo -u postgres pg_dump cms_db > backup.sql

# بازیابی
sudo -u postgres psql cms_db < backup.sql

# دسترسی به دیتابیس
sudo -u postgres psql -d cms_db
```

### تست سیستم
```bash
# تست health check
curl http://localhost/health

# تست اتصال دیتابیس
sudo -u postgres psql -d cms_db -c "SELECT 1;"

# تست Redis
redis-cli ping
```

## 🔒 امنیت

### تنظیمات امنیتی اعمال شده
- ✅ فایروال فعال
- ✅ SSL certificate (خودامضا)
- ✅ کاربر غیر root برای برنامه
- ✅ محدودیت دسترسی فایل‌ها

### تنظیمات امنیتی اضافی (توصیه شده)
```bash
# تغییر رمز admin
sudo -u cms /home/cms/venv/bin/python -c "
from /opt/flask-cms.app import create_app, db, User
app = create_app()
with app.app_context():
    admin = User.query.filter_by(username='admin').first()
    admin.set_password('new-password')
    db.session.commit()
"

# تنظیم fail2ban
dnf install -y fail2ban
systemctl enable fail2ban
systemctl start fail2ban
```

## 📊 مانیتورینگ

### لاگ‌های مهم
- **برنامه:** `journalctl -u flask-cms`
- **PostgreSQL:** `/var/lib/pgsql/15/data/log/`
- **Redis:** `journalctl -u redis6`
- **Nginx:** `/var/log/nginx/`

### اسکریپت مانیتورینگ
```bash
#!/bin/bash
echo "=== Flask CMS Status ==="
systemctl is-active flask-cms postgresql-15 redis6 nginx
df -h /opt/flask-cms /var/lib/pgsql /var/lib/redis
free -h
curl -s http://localhost/health | python3 -m json.tool
```

## 🆘 عیب‌یابی

### مشکلات رایج

#### 1. خطای اتصال دیتابیس
```bash
# بررسی وضعیت PostgreSQL
systemctl status postgresql-15

# بررسی لاگ‌ها
tail -f /var/lib/pgsql/15/data/log/postgresql-*.log
```

#### 2. خطای Redis
```bash
# بررسی وضعیت Redis
systemctl status redis6

# تست اتصال
redis-cli ping
```

#### 3. خطای Nginx
```bash
# تست تنظیمات
nginx -t

# بررسی لاگ‌ها
tail -f /var/log/nginx/error.log
```

#### 4. خطای Python
```bash
# بررسی محیط مجازی
sudo -u cms /home/cms/venv/bin/python --version

# بررسی وابستگی‌ها
sudo -u cms /home/cms/venv/bin/pip list
```

## 📋 چک‌لیست نصب

### قبل از نصب
- [ ] سرور Red Hat 8.10 آماده است
- [ ] حداقل 4GB RAM و 20GB فضای دیسک
- [ ] دسترسی root یا sudo
- [ ] پروژه کپی شده است

### بعد از نصب
- [ ] تمام سرویس‌ها فعال هستند
- [ ] برنامه از طریق http://localhost قابل دسترسی است
- [ ] health check پاسخ می‌دهد
- [ ] می‌توان با admin/admin123 وارد شد

## 🎉 مزایای این روش

- ✅ نصب با یک دستور
- ✅ خودکارسازی کامل
- ✅ پیکربندی امن
- ✅ آماده برای production
- ✅ قابلیت مانیتورینگ
- ✅ پشتیبانی از بکاپ و بازیابی

## 📞 پشتیبانی

برای گزارش مشکل یا درخواست کمک:
- بررسی لاگ‌های سیستم
- اجرای اسکریپت مانیتورینگ
- بررسی وضعیت سرویس‌ها

---

**توسعه داده شده با ❤️ برای نصب آسان**



