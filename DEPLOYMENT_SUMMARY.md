# خلاصه Deployment Flask CMS برای Red Hat 8.10

## 🎯 هدف
نصب و راه‌اندازی Flask CMS روی سرور Red Hat 8.10 **بدون دسترسی اینترنت**

## 📁 فایل‌های ایجاد شده

### 1. راهنماهای نصب
- **`REDHAT_DEPLOYMENT.md`** - راهنمای کامل deployment
- **`README_OFFLINE_DEPLOYMENT.md`** - راهنمای نصب آفلاین
- **`DEPLOYMENT_SUMMARY.md`** - این فایل (خلاصه)

### 2. فایل‌های Requirements
- **`requirements-offline.txt`** - وابستگی‌های Python برای نصب آفلاین
- **`offline-packages-list.txt`** - لیست پکیج‌های RPM مورد نیاز

### 3. اسکریپت‌های نصب
- **`install-redhat.sh`** - اسکریپت نصب اصلی (نیاز به اینترنت)
- **`setup-offline.sh`** - اسکریپت نصب آفلاین (بدون اینترنت)
- **`download-offline-packages.sh`** - اسکریپت دانلود پکیج‌ها

## 🚀 مراحل نصب (خلاصه)

### مرحله 1: آماده‌سازی (روی سرور با اینترنت)
```bash
# 1. دانلود پکیج‌های آفلاین
./download-offline-packages.sh

# 2. ایجاد آرشیو (خودکار)
# فایل: flask-cms-offline-packages-YYYYMMDD_HHMMSS.tar.gz
```

### مرحله 2: انتقال به سرور مقصد
```bash
# کپی آرشیو
scp flask-cms-offline-packages-*.tar.gz user@target-server:/tmp/

# یا کپی کامل پروژه
scp -r flask-cms-itop/ user@target-server:/tmp/
```

### مرحله 3: نصب روی سرور مقصد
```bash
# استخراج آرشیو
cd /tmp && tar -xzf flask-cms-offline-packages-*.tar.gz

# نصب آفلاین
cd offline-packages && sudo ./setup-offline.sh
```

## 📦 پیش‌نیازهای سیستم

### مشخصات سرور
- **سیستم عامل:** Red Hat Enterprise Linux 8.10
- **معماری:** x86_64
- **حداقل RAM:** 4GB (توصیه: 8GB)
- **حداقل فضای دیسک:** 20GB (توصیه: 50GB)

### پکیج‌های اصلی
- Python 3.11 + pip + dev tools
- PostgreSQL 15
- Redis 6
- Nginx
- Development Tools (gcc, make, etc.)

### پورت‌های مورد نیاز
- **80** - HTTP (Nginx)
- **443** - HTTPS (Nginx)
- **5000** - Flask App (داخلی)
- **5432** - PostgreSQL
- **6379** - Redis

## 🔧 تنظیمات خودکار

### سرویس‌های نصب شده
- **PostgreSQL 15** - دیتابیس اصلی
- **Redis 6** - کش و session storage
- **Nginx** - reverse proxy
- **Flask CMS** - برنامه اصلی

### کاربران ایجاد شده
- **cms** - کاربر برنامه (غیر root)
- **cms_user** - کاربر دیتابیس

### دایرکتوری‌های مهم
- **`/opt/flask-cms/`** - برنامه اصلی
- **`/home/cms/venv/`** - محیط مجازی Python
- **`/var/lib/pgsql/15/`** - دیتابیس PostgreSQL
- **`/var/lib/redis/`** - داده‌های Redis
- **`/etc/nginx/`** - تنظیمات Nginx

## 🌐 دسترسی به برنامه

### URL های مهم
- **وب سایت:** http://localhost
- **Health Check:** http://localhost/health
- **Admin Panel:** http://localhost (پس از ورود)

### اطلاعات ورود پیش‌فرض
- **نام کاربری:** admin
- **رمز عبور:** admin123

## 🛠️ دستورات مدیریت

### مدیریت سرویس‌ها
```bash
# وضعیت سرویس‌ها
systemctl status flask-cms postgresql-15 redis6 nginx

# راه‌اندازی مجدد
systemctl restart flask-cms

# متوقف کردن
systemctl stop flask-cms

# مشاهده لاگ‌ها
journalctl -u flask-cms -f
```

### مدیریت دیتابیس
```bash
# بکاپ
sudo -u postgres pg_dump cms_db > backup.sql

# بازیابی
sudo -u postgres psql cms_db < backup.sql

# دسترسی به دیتابیس
sudo -u postgres psql -d cms_db
```

### تست سیستم
```bash
# تست health check
curl http://localhost/health

# تست اتصال دیتابیس
sudo -u postgres psql -d cms_db -c "SELECT 1;"

# تست Redis
redis-cli ping
```

## 🔒 امنیت

### تنظیمات امنیتی اعمال شده
- ✅ فایروال فعال
- ✅ SSL certificate (خودامضا)
- ✅ کاربر غیر root برای برنامه
- ✅ محدودیت دسترسی فایل‌ها
- ✅ CSRF protection فعال

### تنظیمات امنیتی اضافی (توصیه شده)
- تغییر رمزهای پیش‌فرض
- تنظیم fail2ban
- محدود کردن SSH
- تنظیم SELinux

## 📊 مانیتورینگ

### لاگ‌های مهم
- **برنامه:** `journalctl -u flask-cms`
- **PostgreSQL:** `/var/lib/pgsql/15/data/log/`
- **Redis:** `journalctl -u redis6`
- **Nginx:** `/var/log/nginx/`

### اسکریپت مانیتورینگ
```bash
#!/bin/bash
echo "=== Flask CMS Status ==="
systemctl is-active flask-cms postgresql-15 redis6 nginx
df -h /opt/flask-cms /var/lib/pgsql /var/lib/redis
free -h
curl -s http://localhost/health | python3 -m json.tool
```

## 🆘 عیب‌یابی

### مشکلات رایج
1. **خطای اتصال دیتابیس** - بررسی وضعیت PostgreSQL
2. **خطای Redis** - بررسی وضعیت Redis
3. **خطای Nginx** - بررسی تنظیمات و لاگ‌ها
4. **خطای Python** - بررسی محیط مجازی و وابستگی‌ها

### دستورات عیب‌یابی
```bash
# بررسی وضعیت کلی
systemctl status flask-cms postgresql-15 redis6 nginx

# بررسی لاگ‌های برنامه
journalctl -u flask-cms --since "1 hour ago"

# بررسی اتصالات شبکه
netstat -tlnp | grep -E ":(80|443|5000|5432|6379)"

# بررسی فضای دیسک
df -h
```

## 📋 چک‌لیست نصب

### قبل از نصب
- [ ] سرور Red Hat 8.10 آماده است
- [ ] حداقل 4GB RAM و 20GB فضای دیسک
- [ ] دسترسی root یا sudo
- [ ] پکیج‌های آفلاین آماده شده‌اند

### بعد از نصب
- [ ] تمام سرویس‌ها فعال هستند
- [ ] برنامه از طریق http://localhost قابل دسترسی است
- [ ] health check پاسخ می‌دهد
- [ ] می‌توان با admin/admin123 وارد شد
- [ ] بکاپ اولیه ایجاد شده است

## 🎉 نتیجه

با استفاده از این راهنما، می‌توانید Flask CMS را به صورت کامل روی سرور Red Hat 8.10 **بدون نیاز به اینترنت** نصب و راه‌اندازی کنید.

### مزایای این روش
- ✅ نصب کاملاً آفلاین
- ✅ خودکارسازی کامل
- ✅ پیکربندی امن
- ✅ آماده برای production
- ✅ قابلیت مانیتورینگ
- ✅ پشتیبانی از بکاپ و بازیابی

---

**توسعه داده شده با ❤️ برای محیط‌های سازمانی**



